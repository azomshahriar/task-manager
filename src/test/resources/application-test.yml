spring:
  datasource:
    url: jdbc:h2:mem:task_manager;DB_CLOSE_DELAY=-1;
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        temp:
          use_jdbc_metadata_defaults: false

    show-sql: true